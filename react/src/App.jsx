import React from 'react'
import './app.css'

// App de dev (Vite) allégée: on n'affiche plus la liste de projets ici
// pour éviter la duplication de données. La grille de projets est gérée
// côté site statique via assets/data/projects.json.
export default function App(){
  return null
}
